<!-- sections/gift-header.liquid -->

<style>
  @import url('https://fonts.googleapis.com/css2?family=Jost:wght@400;500;700&display=swap');

  .gift-section-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #F5F5F5;
    padding: 24px;
    font-family: 'Jost', sans-serif;
  }
   .image-banner-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: #FFFFFF33;
    padding: 24px;
    font-family: 'Jost', sans-serif;
  }
  .gift-brand {
    font-size: 26px;
    font-weight: 700;
    letter-spacing: 1px;
    color: #000000;
  }
  .title-name{
     display: flex;
  align-items: center;
     font-size: 70px;
    font-weight: 500;
    letter-spacing: 0px;
    color: #000000;
    line-height:100%;
   
  }
  .gift-tagline {
    font-size: 16px;
    color: #000000;
    flex: 1;
    text-align: center;
  }
  .gift-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-size: 14px;
    font-weight: 50;
    padding: 10px 32px;
    background: #ffeb3b;
    color: #111;
    border: none;
    border-radius: 0px;
    text-decoration: none;
    transition: color 0.3s ease;
    overflow: hidden;
    position: relative;
    z-index: 1;
  }
  .gift-button::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    height: 100%;
    width: 0%;
    background: #111;
    transition: width 0.35s cubic-bezier(0.4,0,0.2,1);
    z-index: -1;
  }
  .gift-button:hover::before {
    width: 100%;
  }
  .gift-button:hover {
    color: #fff;
    box-shadow: 0 4px 18px rgba(0,0,0,0.18);
  }
  .gift-arrow {
    font-size: 26px;
    font-weight: 700;
    line-height: 1;
    margin-top:-5px;
    margin-right:-5px;
    display: flex;
    align-items: center;
  }
</style>

<div class="gift-section-row">
  <div class="gift-brand">
    {{ section.settings.brand_name }}
  </div>
  <div class="gift-tagline">
    {{ section.settings.tagline }}
  </div>
  <div>
    <a  class="gift-button" aria-label="{{ section.settings.button_text }}">
      <span>{{ section.settings.gift_button_text }}</span>
      <span class="gift-arrow">&rarr;</span>
    </a>
  </div>
</div>
<div class="image-banner-row">
  
   <div class="title-name">
    {{ section.settings.title_name }}
  </div>

</div>

{% schema %}
{
  "name": "Top Gift",
  "settings": [
    {
      "type": "text",
      "id": "brand_name",
      "label": "Brand Name",
      "default": "TISSO VISON"
    },
    {
      "type": "text",
      "id": "tagline",
      "label": "Tagline",
      "default": "Find the ideal gift for your loved ones."
    },
    {
      "type": "text",
      "id": "gift_button_text",
      "label": "Gift Button Text",
      "default": "CHOOSE GIFT"
    },
    {
      "type": "text",
      "id": "title_name",
      "label": "Title Name",
      "default": "The Gift Guide"
    },
    {
      "type": "text",
      "id": "header_tagline",
      "label": "Header Tagline",
      "default": "Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect gifts to delight your loved ones this holiday season."
    },
    {
      "type": "text",
      "id": "shop_button_text",
      "label": "Shop Button Text",
      "default": "Shop Now"
    }
  ],
  "presets": [
    {
      "name": "Top Gift(custom)"
    }
  ]
}
{% endschema %}
